describe Spree::ProductImport do
  describe '#create' do
    it 'should create a record with :uploaded status'
    it 'should start processing after create'
    # it { expect(subject).to validate_presence_of(:status) }
    # it { expect(subject).to validates_attachment_presense(:data_file) }
  end
end
